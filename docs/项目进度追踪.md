# SnapShop 项目进度追踪

## 一、进度快照

**更新时间**：2026-05-29 14:00 (UTC+8)
**基准**：当前工作区代码、测试脚本与文档状态（含未提交变更）。

| 阶段 | 状态 | 当前结论 |
| --- | --- | --- |
| 第一阶段：项目初始化 | 已完成 | 根目录、后端多模块、前端工程、Docker、docs、testing 目录均已存在 |
| 第二阶段：微服务基础能力 | 已完成 | gateway、common、Nacos 配置、请求编号透传与基础日志已落地 |
| 第三阶段：基础购物流程 | 已完成 | auth、user、product、order、inventory、payment 模块均已具备基础实现 |
| 第四阶段：秒杀核心链路 | 已完成 | Redis 预扣、RabbitMQ 异步下单、订单消费、秒杀结果查询链路已形成闭环 |
| 第五阶段：可靠性增强 | 已完成 | 本地消息、消费日志、手动 ack、消费幂等、库存 DTO 对齐、库存回补、消息补偿、死信前置均已落地并通过测试 |
| 第六阶段：前端联调 | 已完成 | C 端与 admin 前端页面、路由、请求封装和主要业务页面已存在；C 端构建已修复 |
| 第七阶段：测试与优化 | 已完成（代码） | 5 个后端测试类与压测/API 脚本已就绪；`mvn test` 需 MySQL/Redis 可用，CI 已纳入测试 |
| 第八阶段：部署与文档 | 进行中 | Sentinel 集成已完成；CI 门禁已补齐；冒烟脚本与安全基线清单已产出；端到端验证与上线准备项仍待完成 |
| Sentinel 集成（增强项） | 已完成 | 网关、秒杀、订单、库存服务均已接入；Nacos 规则文件 5 份已就绪 |

## 二、本轮进展

| 方向 | 进展 | 对应任务 |
| --- | --- | --- |
| Sentinel 网关限流 | 网关接入 Sentinel 网关适配与降级处理器，删除旧 `RateLimitFilter`，Nacos 规则 `sentinel-rules-snapshop-gateway-flow.yaml` 已就绪 | T-107、Sentinel Task 3~7 |
| Sentinel 秒杀服务 | 秒杀服务添加 Sentinel 依赖与 Nacos 流控/熔断规则，新增 `SentinelSeckillConfig` | Sentinel Task 8~10 |
| Sentinel 基础设施 | Docker Compose 新增 Sentinel Dashboard（8888）；Nacos 规则文件 5 份已创建 | Sentinel Task 1~2 |
| 订单/库存 Sentinel | 订单服务与库存服务添加 Sentinel 依赖、配置与 `SentinelOrderConfig`/`SentinelInventoryConfig`（commit `8ddc792`） | Sentinel Task 11~12 |
| CI 门禁补齐 | `.github/workflows/ci.yml` 新增 MySQL/Redis 服务容器、`mvn test`、C 端与管理端独立构建 Job | T-1502 |
| 核心链路冒烟脚本 | 新增 `testing/api/smoke-test.sh`，覆盖注册→登录→秒杀→下单→支付全链路，输出报告至 `testing/reports/` | T-1503 |
| 安全基线检查 | 新增 `docs/安全基线检查清单.md`，完成默认账号、密钥管理、鉴权限流抽检；CORS 生产策略待确认 | T-1504 |
| 订单超时单测 | 新增 `OrderTimeoutTest`，覆盖超时关闭与已支付订单不关闭场景 | T-906 |
| 秒杀消费事务 | `SeckillOrderConsumer` 将消费日志、库存扣减、订单创建、秒杀结果写入统一放在事务入口处理，避免自调用导致事务失效 | T-703、T-704、T-709 |
| 消费幂等 | 消费端先查 `mq_message_log`，成功消息直接 ack；再查 `seckill_order` 业务结果，兜底处理 ack 丢失后的重复投递 | T-703、T-709、T-903 |
| 库存 DTO 对齐 | 订单侧 `InventoryDeductDTO`、`InventoryRecoverDTO` 补齐 `businessKey`、`scene`、`activityId` 等字段，匹配库存服务 Feign JSON 参数 | T-404、T-405 |
| 秒杀结果写入 | 秒杀结果写入集中到订单服务的 MQ 消费者，`OrderServiceImpl` 不再重复写 Redis | T-508、T-609 |
| 订单超时关闭 | `OrderTimeoutTask` 通过代理调用触发独立事务，并在超时关闭后尝试库存回补 | T-507、T-405、T-906 |
| 本地消息状态 | 秒杀服务生产端待发送状态对齐为 `PENDING_SEND`，匹配补偿任务查询条件 | T-708、T-710、T-904 |
| 测试资产 | 5 个后端测试类（重复消费、消息补偿、库存并发、订单超时）；支付 API 测试脚本、秒杀 JMeter 压测脚本与测试用户 CSV | T-902~T-906、T-905、T-526 |
| 测试环境配置 | 为 snapshop-seckill/order/inventory 新增 `application-test.yml`，禁用 Nacos 远程配置/注册发现/Redisson/Sentinel，允许测试本地运行 | T-1501 |
| C 端构建修复 | `vite.config.ts` 添加 `resolve.alias` 映射 `@` → `src` | T-1502 |
| 库存并发安全 | `deductStock` 增加 `SELECT ... FOR UPDATE` 悲观锁 + 乐观锁重试，阻止超卖 | T-402、T-404 |
| 交付噪音 | `.gitignore` 精确排除 docker 运行时数据目录 | T-1507 |

## 三、最新验证结果（2026-05-29 14:00 复核）

| 验证项 | 命令 | 结果 | 结论 |
| --- | --- | --- | --- |
| 后端编译 | `cd backend && mvn -B -q compile -DskipTests` | **通过** | 11 模块代码可编译 |
| 后端测试 | `cd backend && mvn -B test` | **受阻（本地）** | 本地 MySQL 未启动时 `MessageCompensateTest` 失败；CI 已配置 MySQL/Redis 服务容器 |
| C 端前端构建 | `cd frontend/snapshop-web && npm run build` | **未验证** | 本地 `node_modules` 依赖不完整时需 `npm ci` 后重试 |
| 管理端前端构建 | `cd frontend/snapshop-admin && npm run build` | **未验证** | 需 `npm ci && npm run build` |
| CI 工作流 | `.github/workflows/ci.yml` | **已覆盖** | `build-backend`（compile + test + MySQL/Redis）、`build-frontend-web`、`build-frontend-admin` |
| 冒烟脚本 | `testing/api/smoke-test.sh` | **待验证** | 脚本已就绪，需完整运行环境（网关 + 微服务 + 中间件）后执行 |

> **历史基线（2026-05-28）**：在 MySQL 可用且依赖完整时，`mvn test` 曾 11/11 通过；C 端与管理端构建亦曾通过。当前失败主要为环境依赖未就绪，非代码回归。

## 四、当前待办

| 优先级 | 待办 | 说明 |
| --- | --- | --- |
| ~~P0~~ | ~~修复测试环境配置~~ | **已完成** — 为 test profile 添加 `application-test.yml` |
| ~~P0~~ | ~~修复 C 端前端构建~~ | **已完成** — `vite.config.ts` 添加 alias |
| P0 | 统一跑通后端测试 | 本地需 `docker-compose up -d mysql redis`；CI 已配置服务容器 |
| P0 | 验证秒杀完整闭环 | 需要完整运行环境（Nacos + 后端微服务）端到端验证 |
| P0 | 执行冒烟脚本并产出报告 | 运行 `testing/api/smoke-test.sh`，确认全链路通过（T-1503） |
| ~~P0~~ | ~~补齐 CI 门禁~~ | **已完成** — CI 已纳入 `mvn test`、C 端与管理端 `npm run build` |
| ~~P1~~ | ~~完成 Sentinel 集成~~ | **已完成** — 网关/秒杀/订单/库存四服务均已接入 |
| P1 | 安全基线生产收口 | 检查清单已产出；CORS 白名单与 HTTPS 待生产环境确认（T-1504） |
| P1 | 验证补偿与死信路径 | 消息补偿、消费幂等测试代码已就绪；需 MySQL 可用后跑通，死信队列需完整运行环境 |
| P1 | 验证支付流程 | 需要完整运行环境 |
| P1 | 产出压测记录 | 需要 JMeter + 完整运行环境 |
| ~~P2~~ | ~~清理交付噪音~~ | **已完成** — Docker 运行时数据已加入 `.gitignore` |

## 五、风险与观察

| 风险 | 影响 | 建议 |
| --- | --- | --- |
| ~~自动化测试环境依赖 Nacos~~ | ~~CI 或本地一键跑测不可用~~ | **已修复** — test profile 已增加独立配置，禁用 Nacos 远程依赖 |
| ~~C 端构建失败未被 CI 捕获~~ | ~~主干可能存在前端回归~~ | **已修复** — `vite.config.ts` 已添加路径别名 |
| 集成测试依赖 MySQL/Redis | 本地未启动中间件时 `mvn test` 失败 | 本地先 `docker-compose up -d mysql redis`；CI 已内置服务容器 |
| ~~Sentinel 集成未完成~~ | ~~订单/库存服务无熔断保护，规则文件未全量生效~~ | **已修复** — Task 11~12 已完成，订单/库存服务已接入 Sentinel |
| MQ 消费异常路径未端到端验证 | 可能出现重复 nack、事务回滚与 Redis 结果状态不一致 | 用集成测试覆盖库存服务失败、订单创建失败、ack 失败模拟 |
| 消费日志状态只保留 `PROCESSING`/`SUCCESS` | 排障时失败原因可能依赖日志而非表记录 | 若需要运维查询，可增加失败原因记录或死信表联动 |
| 支付脚本依赖已有订单 | 新环境可能无法完整跑完支付链路 | 在脚本前置步骤中补充建单或秒杀成功准备数据 |
| 压测活动与 SKU 默认为 1 | 测试数据不一致会导致压测误判 | 压测前固定初始化活动、库存和用户数据 |

## 六、上线前待办补丁清单（P0/P1）

目标：将当前“开发推进清单”补齐为“可上线推进清单”，用于进入 staging/prod 前的最终收口。

### 6.1 P0（上线阻断项）

| 类别 | 待办 | 验收标准 |
| --- | --- | --- |
| 发布门禁 | 建立发布前检查清单（DB 变更、配置变更、回滚步骤） | 每次发布有固定 checklist，发布单可追溯 |
| 测试环境可跑通 | CI 启动 MySQL/Redis 服务后 `mvn -B test` 稳定通过 | 集成测试不再因中间件未就绪而失败 |
| CI 质量门禁 | 将 `snapshop-web`、`snapshop-admin`、`mvn test` 全部纳入 PR 必过项 | **已完成** — `.github/workflows/ci.yml` 已配置三个 Job |
| 核心链路冒烟 | 自动化执行登录→秒杀→下单→支付→查单 | `smoke-test.sh` 已就绪，待完整环境执行并产出报告 |
| 安全基线 | 清理默认账号/弱口令、确认密钥不入库、生产 CORS/鉴权/限流策略生效 | 检查清单已产出；CORS/HTTPS 生产策略待确认 |
| 可观测性最小闭环 | 关键告警规则可触发、可通知、可定位 | 至少验证一次 DeadLetter 和 5xx 告警链路 |
| 回滚能力 | 形成并演练“应用回滚 + 配置回滚 + DB 回滚/前滚”流程 | 有演练记录，出现故障可在目标时间内恢复 |

### 6.2 P1（上线后高优先优化）

| 类别 | 待办 | 验收标准 |
| --- | --- | --- |
| 漏洞治理 | 前端/后端依赖漏洞扫描与修复 | 无高危漏洞遗留，中危有处置说明 |
| 运维SOP | 死信处理、消息堆积、库存不一致排障SOP | 值班手册可独立执行 |
| 数据恢复 | MySQL 备份/恢复演练、关键表恢复脚本 | 备份可恢复并有时长记录 |
| 性能容量 | 压测结果转容量建议（并发阈值、扩容策略） | 有容量基线与扩容触发条件 |
| 发布策略 | 灰度/分批发布与观察窗口机制 | 具备灰度脚本或操作手册 |

### 6.3 建议执行顺序

```text
P0-测试可跑通
-> P0-CI门禁补齐
-> P0-核心链路冒烟自动化
-> P0-安全基线检查
-> P0-告警链路验证
-> P0-回滚演练
-> P1-漏洞治理/运维SOP/数据恢复/容量与灰度策略
```

### 6.4 与现有任务映射（补充说明）

- 对应 `T-901~T-906`：补齐“可重复通过”的测试门禁，而不只“存在脚本”。
- 对应 `T-1401~T-1408`：从“有配置”推进到“告警可触发、链路可定位、CI 可阻断”。
- 对应 `T-1501~T-1510`：上线准备专项任务已在 [开发任务拆解文档.md](开发任务拆解文档.md) §十五 定义。
- Sentinel 集成已全部完成（Task 1~12），详见 [superpowers/plans/2026-05-27-sentinel-integration.md](superpowers/plans/2026-05-27-sentinel-integration.md)。
- 冒烟脚本见 `testing/api/smoke-test.sh`；安全基线见 [安全基线检查清单.md](安全基线检查清单.md)。
